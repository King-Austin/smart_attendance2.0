# Set these as Supabase Edge Function secrets; never expose them to Expo.
BIOMETRIC_API_URL=https://your-biometric-api.example.com
# Optional when the biometric service is protected by an API key.
BIOMETRIC_API_KEY=your-biometric-api-key
FACE_DUPLICATE_THRESHOLD=0.65
